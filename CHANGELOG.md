# 개발 기록 (똑똑치과상담실장)

치과 상담 웹앱. 단일 파일 구조 — `index.html` 하나에 CSS/HTML/JS가 전부 인라인으로 들어있음(약 6,000줄). 별도 빌드 과정 없이 바로 배포되는 정적 페이지.

이 문서는 오랜만에 프로젝트로 돌아왔을 때 "지금까지 뭘 했었는지" 빠르게 파악하기 위한 기록입니다. 상세 diff는 `git log` 참고.

---

## 주요 기능

- **치료 상담**: 캔버스에 그림을 그리며 설명, STT(음성인식)로 상담 내용 자동 기록, 세션 종료 시 자동 저장
- **비용 상담**: 카테고리별 항목 선택 → 견적 작성, 항목별/전체 할인, 견적 A/B 비교 모드
- **환자 정보**: 환자 등록/조회, 상담 이력 확인
- Supabase 연동 (로그인, 데이터 저장/조회)

---

## 지금까지 작업 히스토리 (주제별)

### 1. 모바일 대응
- 모바일(768px 이하)에서는 치료 상담 캔버스 화면을 숨기고 "PC에서 이용해주세요" 안내 메시지 표시. 태블릿(834px 이하)/PC는 그대로 캔버스 사용.
- 화면 회전/창 크기 변경 시에도 즉시 상태 갱신되도록 `resize` 이벤트 처리 추가.

### 2. STT(음성인식)
- 말하다 잠시 멈추면 인식이 끊기던 버그 수정 (`onerror`/`onend` 중복 재시작 충돌이 원인).
- 상담 요약을 저장 시 자동 실행하지 않고, 사용자가 "AI 요약" 버튼을 눌렀을 때만 실행하도록 변경.
- "STT 원본 보기" 영역 기본값을 펼침 상태로 변경.
- Safari에서 `audio/webm` 미지원으로 녹음 파일이 생성되지 않던 문제 수정 (지원 포맷 자동 감지).

### 3. 비용 상담 화면 개편
- PC에서 화면이 지나치게 넓어 보이던 문제 → 좌(항목 선택+즐겨찾기, 450px) / 우(견적+합계) 2컬럼 레이아웃으로 변경.
- 자주 쓰는 항목 즐겨찾기(⭐) 기능, 항목 드래그로 순서 변경 기능 추가.
- 항목 행에 단가 컬럼 표시 추가.
- **항목별 할인 기능** 신규 구현: `[5%][10%][15%][20%][금액입력]` 프리셋 버튼 + 실시간 미리보기. 할인 적용 시 원가는 취소선, 할인가는 파란색으로 표시. 여러 차례 사용성 피드백을 받아 UI를 다듬음(한 줄 배치, 버튼 색상, 정렬 등).

### 4. 전체 코드 점검 (버그 수정 1차)
- 상담 저장 시 미정의 변수 참조로 저장이 실패하던 치명적 버그 수정 (당시엔 몰랐지만, 사실 이 함수 자체가 죽은 코드였음 → 아래 5번 참고)
- 상담 이력 날짜 표시 깨짐 (`26.01.15.` → `2601.15.`) 수정
- 캔버스 템플릿 전환 시 이전 그림이 안 지워지고 남던 버그 수정
- 생년월일 선택 모달 이벤트 리스너 중복 누적 문제 수정
- "오늘 상담 N명" 카운트가 UTC 기준이라 새벽 시간대(자정~오전9시) 상담이 누락되던 문제 수정 (KST 기준으로 변경)
- 호출되지 않는 죽은 함수(`setGender`, `createDiscountRow`) 삭제

### 5. 코드 정리 + 견적 저장 누락 버그 발견/수정 (가장 최근 작업)
전체 코드를 다시 훑어보다가 **중요한 버그**를 발견함:

> **상담을 종료할 때 실제로 실행되는 저장 함수(`saveConsultOnEnd`)가 비용 상담 탭에서 만든 견적(항목/수량/할인)을 전혀 저장하지 않고 있었음.** `estimated_total`이 `0`으로 하드코딩되어 있었고 `consultation_items` 테이블에 아무것도 insert하지 않았음.
>
> 원인: 견적 저장 로직은 `saveConsultation`이라는 함수에 있었는데, 이 함수는 예전 "팝업 상담 시스템"이 제거되면서(→ 캔버스 세션 방식으로 전환) 실제로는 어디서도 호출되지 않는 죽은 코드가 되어 있었음. (1차 점검 때 이 죽은 함수의 다른 버그를 고쳤었는데, 그때는 이게 호출 안 되는 함수인 줄 몰랐음)
>
> **조치**: 견적 계산(`calcQuote`)과 항목별 할인 저장 로직을 실제 저장 함수 `saveConsultOnEnd`에 병합. 상담 종료 시 이제 견적 총액, 항목별 할인, 환자의 `last_total`까지 정상 저장됨.

그 외 정리 작업:
- **죽은 코드 삭제** (전부 호출부 0건 확인 후 삭제): `saveConsultation`, `openMgmtPanel`/`closeMgmtPanel`, `setTab`, `updateCatDropdownLabel`, `updateEstimatePreview`, `loadStrokesFromDB`, 빈 스텁 함수 `saveCanvasHistory`(+호출부 4곳)
- **중복 로직 통합**: 단일모드/비교모드용으로 나뉘어 있던 견적 항목 CRUD 함수(`removeCostItem`/`removeCostItemQ`, `changeCostItemQty`/`changeCostItemQtyQ`) 통합. 즐겨찾기 영역과 카테고리 항목 목록의 버튼 렌더링 로직을 공통 함수로 추출.
- **매직 넘버 정리**: 모바일/태블릿 breakpoint(768/834)를 `MOBILE_BREAKPOINT`/`TABLET_BREAKPOINT` 상수로 선언 (CSS 쪽엔 동기화 필요하다는 주석 추가).
- **불필요한 레거시 주석 제거**
- **무관한 파일 삭제**: `assets/`, `prompt.txt`, `README_SPLIT_GUIDE.md` — 치과 상담 앱과 무관한 다른 프로젝트("ScanHero" RPG 게임)의 잔재였음. `index.html`이 전혀 참조하지 않는 것을 확인 후 삭제.

---

## 알려진 이슈 / 남겨둔 과제

이번 정리에서 발견했지만 **의도적으로 손대지 않은** 것들. 판단이 필요하거나 블라스트 반경이 커서 사용자 확인 후 진행하는 게 맞다고 생각한 항목들.

### ⚠️ 보안: `loadPatients`에 `user_id` 필터 없음
- 여러 계정이 이 앱을 함께 쓰게 되면, 다른 계정의 환자 목록이 보일 수 있는 구조.
- 예전에 이 필터를 코드에 넣었다가 "환자 목록이 빈 화면으로 나오는" 버그가 나서 일부러 제거한 이력이 있음(`4a730ec`). 코드로 재추가하면 그 버그가 재발할 가능성이 있어, **Supabase RLS(Row Level Security) 정책으로 처리하는 게 정석**. 1인/1계정으로만 쓴다면 당장 급하지 않지만, 다계정 전환 시 반드시 확인 필요.

### 🔍 진단 메모(텍스트박스) 직접 입력 시 저장 안 됨
- "상담 내용을 메모하세요" textarea(`consultDiagnosisInput`)에 사용자가 직접 타이핑한 내용은, AI 요약을 실행하지 않으면 저장되지 않음. 현재 저장 로직은 `AI 요약 결과 || STT 원문`만 `diagnosis_note`로 저장하고 textarea 값 자체는 안 읽음.
- 의도된 동작인지, textarea 값도 함께 저장해야 하는지 확인 필요.

### 🧹 더 큰 구조 개선 후보 (분석은 했으나 실행 안 함 — 리스크 대비 실익 판단 필요)
- `bindEvents`(225줄), `bindCanvasEvents`(270줄), `loadConsultations`(182줄) 같은 거대 함수를 기능 단위로 분해
- `AppState` 외에 산재된 모듈 전역 변수들(`birthCallerContext`, `popSnsType`, `bgOpacity`, `quotes`/`activeQuoteIdx` 등)을 상태 객체로 통합
- 모달 open/close 패턴(8쌍 이상 반복)을 공통 헬퍼로 통합
- `renderCostItems`(단일모드) / `renderCompare`(비교모드) 렌더링 로직 중복 축소
- 하드코딩 색상(`#dc2626`, `#2563eb` 등)을 CSS 변수로 승격
- 이벤트 바인딩 방식(`addEventListener` / 인라인 `onclick` / `el.onclick=` 대입) 혼용 → 하나로 통일

---

## 파일 구조

```
index.html   ← 앱 전체 (HTML+CSS+JS 인라인, 유일한 실제 소스 파일)
CHANGELOG.md ← 이 파일
```

`assets/`, `prompt.txt`, `README_SPLIT_GUIDE.md`는 삭제됨 (무관한 프로젝트 잔재, 위 5번 항목 참고).

---

*최종 업데이트: 커밋 `ce7f615` 기준*
